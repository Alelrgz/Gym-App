import 'dart:typed_data';
import 'package:flutter/material.dart';

/// Captures a photo for chat. Returns JPEG bytes or null if cancelled.
/// This is the stub — the actual implementation is selected via conditional import.
Future<Uint8List?> capturePhotoForChat(BuildContext context) {
  throw UnsupportedError('capturePhotoForChat not implemented for this platform');
}
